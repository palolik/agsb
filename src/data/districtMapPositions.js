// Shared coordinate space for public/assets/BD_Map_dark.svg.
// Per-district pin positions now live in allDistricts.js (all 64 districts),
// read directly off this SVG's own district-name labels.
export const MAP_VIEWBOX = { width: 1530.748, height: 2138 };
